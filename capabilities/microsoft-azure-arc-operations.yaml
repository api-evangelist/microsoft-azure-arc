naftiko: 1.0.0-alpha2
info:
  label: Azure Arc Hybrid Compute REST API — Operations
  description: 'Azure Arc Hybrid Compute REST API — Operations. 1 operations. Lead operation: Azure Arc Hybrid Compute REST
    API List operations. Self-contained Naftiko capability covering one Microsoft Azure Arc business surface.'
  tags:
  - Microsoft Azure Arc
  - Operations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_ARC_API_KEY: MICROSOFT_AZURE_ARC_API_KEY
capability:
  consumes:
  - type: http
    namespace: microsoft-azure-arc-operations
    baseUri: https://management.azure.com
    description: Azure Arc Hybrid Compute REST API — Operations business capability. Self-contained, no shared references.
    resources:
    - name: providers-Microsoft.HybridCompute-operations
      path: /providers/Microsoft.HybridCompute/operations
      operations:
      - name: listoperations
        method: GET
        description: Azure Arc Hybrid Compute REST API List operations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.MICROSOFT_AZURE_ARC_API_KEY}}'
  exposes:
  - type: rest
    namespace: microsoft-azure-arc-operations-rest
    port: 8080
    description: REST adapter for Azure Arc Hybrid Compute REST API — Operations. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/providers/microsoft-hybridcompute/operations
      name: providers-microsoft-hybridcompute-operations
      description: REST surface for providers-Microsoft.HybridCompute-operations.
      operations:
      - method: GET
        name: listoperations
        description: Azure Arc Hybrid Compute REST API List operations
        call: microsoft-azure-arc-operations.listoperations
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: microsoft-azure-arc-operations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Azure Arc Hybrid Compute REST API — Operations. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: azure-arc-hybrid-compute-rest
      description: Azure Arc Hybrid Compute REST API List operations
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: microsoft-azure-arc-operations.listoperations
      outputParameters:
      - type: object
        mapping: $.
